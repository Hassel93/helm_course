Create a namespace "test" and install the wordpress chart again but in this new namespace.
Use the normal helm list again. What do you see and which command do you need to list your installed release ?
What are the consequences ? What does this mean for the visibility of helm releases ?